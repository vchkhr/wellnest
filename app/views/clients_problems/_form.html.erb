<%= form_with(model: clients_problem) do |form| %>
  <% if clients_problem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(clients_problem.errors.count, "error") %> prohibited this clients_problem from being saved:</h2>

      <ul>
        <% clients_problem.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :client_id %>
    <%= form.text_field :client_id %>
  </div>

  <div class="field">
    <%= form.label :problem_id %>
    <%= form.text_field :problem_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
