<%= form_with(model: coaches_problem) do |form| %>
  <% if coaches_problem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coaches_problem.errors.count, "error") %> prohibited this coaches_problem from being saved:</h2>

      <ul>
        <% coaches_problem.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :coach_id %>
    <%= form.text_field :coach_id %>
  </div>

  <div class="field">
    <%= form.label :problem_id %>
    <%= form.text_field :problem_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
