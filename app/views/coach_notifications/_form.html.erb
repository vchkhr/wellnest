<%= form_with(model: coach_notification) do |form| %>
  <% if coach_notification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coach_notification.errors.count, "error") %> prohibited this coach_notification from being saved:</h2>

      <ul>
        <% coach_notification.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text %>
  </div>

  <div class="field">
    <%= form.label :coach_id %>
    <%= form.text_field :coach_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
