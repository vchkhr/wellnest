<p id="notice"><%= notice %></p>

<h1>Clients Techniques</h1>

<table>
  <thead>
    <tr>
      <th>Client</th>
      <th>Technique</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @clients_techniques.each do |clients_technique| %>
      <tr>
        <td><%= clients_technique.client_id %></td>
        <td><%= clients_technique.technique_id %></td>
        <td><%= link_to 'Show', clients_technique %></td>
        <td><%= link_to 'Edit', edit_clients_technique_path(clients_technique) %></td>
        <td><%= link_to 'Destroy', clients_technique, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Clients Technique', new_clients_technique_path %>
