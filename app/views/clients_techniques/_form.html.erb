<%= form_with(model: clients_technique) do |form| %>
  <% if clients_technique.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(clients_technique.errors.count, "error") %> prohibited this clients_technique from being saved:</h2>

      <ul>
        <% clients_technique.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :client_id %>
    <%= form.text_field :client_id %>
  </div>

  <div class="field">
    <%= form.label :technique_id %>
    <%= form.text_field :technique_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
