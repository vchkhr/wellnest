<%= render 'layouts/navigation/app', user: current_user %>

<div class="card-view">
  <p id="alert"><%= alert %></p>
  <p id="notice"><%= notice %></p>

  <%= render 'layouts/coach', user: @coach.user, full_info: true %>
</div>
